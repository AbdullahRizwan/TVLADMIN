<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Admin">
        <activity android:name=".View.Associate_Part"></activity>
        <activity android:name=".View.AssociateVehicle" />
        <activity android:name=".View.Add_Part" />
        <activity android:name=".View.Add_Vehicle" />
        <activity android:name=".View.Profile" />
        <activity android:name=".View.HomeScreen" />
        <activity android:name=".View.Signup" />
        <activity android:name=".View.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>